[documentRoot]
root = "/path/to/appBackend"

[paths]
data = "/path/to/appBackend/backend/data"
data_point = "/path/to/appBackend/backend/data/users"

[files]
user_file = "/path/to/appBackend/backend/data/users.json"


Currently using absolute paths to certain directories and filling the rest with relative path depending on the api call